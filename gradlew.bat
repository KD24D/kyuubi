@rem Add standard gradlew.bat script content.
@rem Similar to gradlew, this is a placeholder.
@echo off
echo Attempting to execute Gradle wrapper (Windows)...
echo If this fails, the full gradlew.bat script needs to be installed.
echo Gradle wrapper properties file is expected at gradle\wrapper\gradle-wrapper.properties
